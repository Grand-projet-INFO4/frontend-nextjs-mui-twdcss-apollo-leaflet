// URL query param for the reason behind a redirection
export const REDIRECTION_REASON_QUERY_PARAM = "redirect";

// URL query param for the current page url where that triggers the redirection
export const REDIRECTION_PREVIOUS_URL_QUERY_PARAM = "from";
